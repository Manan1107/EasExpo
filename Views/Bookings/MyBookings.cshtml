@model IEnumerable<BookingListItemViewModel>
@using System
@{
    ViewData["Title"] = "My bookings";
}

<h1 class="h3 mb-3">My bookings</h1>

@if (!Model.Any())
{
    <div class="alert alert-info">You haven't booked any stalls yet. Browse <a asp-controller="Events" asp-action="Index">available events</a> to get started.</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="thead-light">
                <tr>
                    <th>Event</th>
                    <th>Stall slot</th>
                    <th>Event dates</th>
                    <th>Status</th>
                    <th>Payment</th>
                    <th>Amount</th>
                    <th class="text-right">Actions</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var booking in Model)
            {
                <tr>
                    <td>
                        <div class="font-weight-semibold">@(!string.IsNullOrWhiteSpace(booking.EventName) ? booking.EventName : booking.StallName)</div>
                        @if (!string.IsNullOrWhiteSpace(booking.EventName))
                        {
                            <div class="small text-muted">@booking.StallName</div>
                        }
                    </td>
                    <td>@(booking.SlotNumber > 0 ? $"Slot {booking.SlotNumber}" : "—")</td>
                    <td>@booking.StartDate.ToString("dd MMM") - @booking.EndDate.ToString("dd MMM yyyy")</td>
                    <td>
                        <span class="badge badge-pill @(booking.Status == BookingStatus.Approved ? "badge-success" : booking.Status == BookingStatus.Pending ? "badge-warning" : booking.Status == BookingStatus.Rejected ? "badge-danger" : "badge-secondary")">
                            @booking.Status
                        </span>
                    </td>
                    <td>
                        <span class="badge badge-pill @(booking.PaymentStatus == PaymentStatus.Completed ? "badge-success" : booking.PaymentStatus == PaymentStatus.Pending ? "badge-warning" : "badge-danger")">
                            @booking.PaymentStatus
                        </span>
                    </td>
                    <td>₹@booking.Amount.ToString("N2")</td>
                    <td class="text-right">
                        @if (booking.Status == BookingStatus.Approved && booking.PaymentStatus != PaymentStatus.Completed)
                        {
                            <a asp-action="Pay" asp-route-id="@booking.Id" class="btn btn-sm btn-primary">Pay now</a>
                        }
                        @if (booking.Status == BookingStatus.Approved && booking.PaymentStatus == PaymentStatus.Completed)
                        {
                            <a asp-action="Feedback" asp-route-bookingId="@booking.Id" class="btn btn-sm btn-outline-secondary ml-2">Leave feedback</a>
                        }
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}
